# Generate a secure token with: openssl rand -base64 32
# openssl rand -base64 32 | tr '+/' '-_' | tr -d '=' | sed 's/^/sk_ollama_/'

# Ollama API Configuration
# Generate a secure token with: openssl rand -base64 32
OLLAMA_API_KEY=sk_ollama_api_example_key
  # Replace with your generated token 